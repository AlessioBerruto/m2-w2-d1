using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Esercizio3 : MonoBehaviour
{
    public int a = 3;   // primo metodo per visualizzare una variabile nell'inspector, ossia dichiarando che la variabile   public
    [SerializeField] int b = 4; //secondo metodo per visualizzare una variabile nell'inspector, aggiungendo [SerializeField] prima della variabile

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
